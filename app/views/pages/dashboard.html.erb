<div class="container mt<div class="col my-3">
  <div class="card h-100 shadow-sm my-4">
    <div class="card">

      <% @favorite_recipes.each do |recipe| %>
        <%= link_to recipe_path(recipe), recipe.name %>
        <%=recipe.name%>
        <br>
        <br>
        <%=recipe.description%>
      <%end %>

    </div>
  </div>
</div>
