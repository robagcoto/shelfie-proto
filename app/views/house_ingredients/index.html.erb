<div class="container">
  <div class="mt-3">
    <h1><%= @house.name %> ingredients</h1>
    <ul>
      <% @house_ingredients.each do |ingredient| %>
        <li><%= ingredient.ingredient.name %>: <%= ingredient.quantity %> <%= ingredient.unit %> </li>
      <% end %>
    </ul>
  </div>
</div>
