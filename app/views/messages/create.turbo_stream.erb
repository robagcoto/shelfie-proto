<%= turbo_stream.append "messages" do %>
  <%= render "messages/messages", messages: [@chat.messages.last(2).first] %>
<% end %>

<%= turbo_stream.replace "form" do %>
  <%= render "messages/form", chat: @chat, message: Message.new %>
<% end %>
