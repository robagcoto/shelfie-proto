<%= turbo_stream.append "messages" do %>
  <%= render "messages/message", message: @chat.messages.last(2).first %>
<% end %>

<%= turbo_stream.append "messages" do %>
  <%= render "messages/message", message: @chat.messages.last %>
<% end %>



<%= turbo_stream.update "form" do %>
  <%= render "messages/form", chat: @chat, message: Message.new %>
<% end %>
